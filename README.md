# Getting started

```sh
git clone https://github.com/404-DriverNotFound/ft_transcendence-b.git \
&& cd ft_transcendence-b \
&& git submodule init \
&& git submodule update --remote \
&& docker-compose up --build
```
